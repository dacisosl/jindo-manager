# 작업 방식

- 작업은 로컬 브랜치에서 하고, 끝나면 `main`에 머지해 **`main`만 푸시한다.** PR은 만들지 않는다.
- 작업 브랜치는 원격에 올리지 않는다 — 깃허브에는 `main` 하나만 둔다.
- `main`에 푸시하면 GitHub Actions가 GitHub Pages로 자동 배포된다. 푸시가 곧 배포이므로,
  푸시 전에 `npm run build`가 통과하는지, 바꾼 화면이 실제로 동작하는지 확인하고 올린다.
